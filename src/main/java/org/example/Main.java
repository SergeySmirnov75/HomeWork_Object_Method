package org.example;
/*
public class Person {
    // Создали новый класс Person (Личность)
    String name;
    // Объявили свойство name (имя)
    int age;
    // Объявили свойство age (возраст)
}
*/

public class Main {
    public static void main(String[] args)
    {
        /*
        PersonService personService = new PersonService();
        // Создаем объект PersonService, чтобы использовать его метод
        Person sarah = new Person("Sarah", 30);
        if (personService.isAdult(sarah)) {
            System.out.println("Иди в бар");
        } else {
            System.out.println("Иди в школу");
        }
        sarah.increaseAge(1);
        System.out.println(sarah);
        */

        //  toString, equals и hashCode в классах Author и Book

        Author author = new Author();
        Book book     = new Book();


    }



}